module.exports = {
    testEnvironment: 'node',
    testTimeout: 10000,
    // Force Jest to exit after all tests complete
    forceExit: true,
    // Detect open handles but don't warn about them until after tests
    detectOpenHandles: true
  };